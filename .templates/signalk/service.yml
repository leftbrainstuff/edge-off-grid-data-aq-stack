containerNameGoesHere:
    container_name: signalk
    restart: unless-stopped
    image: signalk/core:latest
    ports:
      - "8086:8086" # Always ensure the WUI port is first in the ports list.
      - "8088:8088"
    volumes:
       - ./volumes/signalk/:/opt/signalk/

